* 'null?' should be procedure
* (define (id arg ...) body ...+)
* eval, quasiquote
* sublis (substitution)
* environment: C, make, automake, autoconf, git, bash, unicode, editor, keyboard, network, display, asm-i386.h
* overload representation of pairs?
* evaluation of strings to simplify code and for implementing test suite
* macros
* library and interpreters with different debug output
* infinite recursion ((x 1 1) 1 0)
